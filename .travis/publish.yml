script:
- npm config set registry @smartrecruiters:https://registry.npmjs.org
- npm config set //registry.npmjs.org/:_authToken ${NPM_API_TOKEN}
- git checkout main
- git remote set-url origin https://${GH_ACCESS_TOKEN}@github.com/smartrecruiters/tsconfig.git
- lerna publish prepatch --no-private --yes
# - git push --follow-tags --no-verify origin main
