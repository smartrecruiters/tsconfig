script:
- git remote set-url origin https://${GH_ACCESS_TOKEN}@github.com/smartrecruiters/tsconfig
- git checkout main
- lerna publish patch --no-private --yes --registry https://registry.npmjs.org/:_authToken=${NPM_API_TOKEN}
